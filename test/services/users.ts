import { MongooseModel } from "@tsed/mongoose";
import assert from "assert";
// import { UsersModel } from "../../db/users";
// import { UsersService } from "../../services/users";

// context("UsersService", () => {

//   const usersModel: MongooseModel<UsersModel> = new UsersModel();
//   const usersTable = new UsersService(usersModel);

//   context("/create", async () => {

//     it("success", async () => {
//       const user = await  .signup(client, body);
//       assert.equal(!!answer.access_token, true);
//     });

//     it("error", () => {
//       assert.rejects(async () => {
//         await as.signup({ id: "", secret: "" }, { email: "", password: "", connection: "" });
//       }, HttpError);
//     });

//   });
// });
